# validators

Zod schemas describing request shapes, used with `middleware/validate`. Group by
resource (for example `auth.validator.js`). Each schema validates some of
`{ body, query, params }`.

No validators yet. This is structure only.
